#ifndef FISHERYATES_H
#define FISHERYATES_H
#include "types.hpp"

void FisherYatesShuffle(Card* streamHead, unsigned int shuffleRange) {};
// we also need rng (trackable source of randomness), how many shuffles (partial-howmany / full)

#endif